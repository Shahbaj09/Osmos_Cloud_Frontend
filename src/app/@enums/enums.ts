export enum Store {
  token = 'token',
  role = 'role',
  userData = "userData",
  categoryData = "categoryData",
  socialUserData = "socialUserData",
  firebaseToken = "firebaseToken",
  fbUid = "fbUid",
  calendarToken = "calendarToken",
  openedChatBoxes = "openedChatBoxes",
}
